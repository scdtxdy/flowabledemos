server:
  port: 8080

flowable:
  #关闭定时任务JOB
  async-executor-activate: false
  #  将databaseSchemaUpdate设置为true。当Flowable发现库与数据库表结构不一致时，会自动将数据库表结构升级至新版本。
  database-schema-update: true


spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/flowable-spring-boot?serverTimezone=Asia/Shanghai&characterEncoding=utf8&useSSL=false&zeroDateTimeBehavior=convertToNull&nullCatalogMeansCurrent=true
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: 542846492
    type: com.alibaba.druid.pool.DruidDataSource